# Upgrade v7.0.0 - Native token mode removal

-   **Date**: YYYY-MM-DD
-   **Git tag**: [v7.0.0](https://github.com/iExecBlockchainComputing/PoCo/releases/tag/v7.0.0)
-   **Upgrade script**: `./v7.0.0.ts`
-   **Upgrade GitHub Action run**:
    -   **Testnet**: [run id (TODO)](https://github.com/iExecBlockchainComputing/PoCo/actions/runs/<runId>)
    -   **Mainnet**: [run id (TODO)](https://github.com/iExecBlockchainComputing/PoCo/actions/runs/<runId>)

## Summary of changes

Every PoCo facet whose runtime bytecode changed since v6.2.0 is replaced. The
diamond proxy, the core diamond facets (`DiamondCutFacet`, `DiamondLoupeFacet`,
`OwnershipFacet`), the registries and the already deployed `IexecLibOrders_v5`
library are left untouched.

### Facet list changes

| Facet                          | Runtime bytes on-chain -> built | Action                              |
| ------------------------------ | ------------------------------- | ----------------------------------- |
| `IexecAccessorsABILegacyFacet` | 4241 -> 4241                    | Left in place, metadata hash only   |
| `IexecCategoryManagerFacet`    | 1915 -> 1738                    | Replaced                            |
| `IexecConfigurationExtraFacet` | 716 -> 562                      | Replaced                            |
| `IexecConfigurationFacet`      | 3559 -> 3473                    | Replaced                            |
| `IexecEscrowTokenFacet`        | 6611 -> removed                 | Renamed to `IexecEscrowFacet`       |
| `IexecEscrowFacet`             | new                             | New name of `IexecEscrowTokenFacet` |
| `IexecOrderManagementFacet`    | 6803 -> 6803                    | Replaced                            |
| `IexecPoco1Facet`              | 13583 -> 13525                  | Replaced                            |
| `IexecPoco2Facet`              | 15080 -> 15065                  | Replaced                            |
| `IexecPocoAccessorsFacet`      | 10789 -> 10793                  | Replaced                            |
| `IexecRelayFacet`              | 1774 -> 1774                    | Left in place, metadata hash only   |

`IexecAccessorsABILegacyFacet` and `IexecRelayFacet` are byte-identical to what
is already deployed once the metadata hash is stripped, so they are neither
removed nor redeployed.

No function selector is lost: the `receive` (`0x00000000`) and `fallback`
(`0xffffffff`) selectors move from `IexecEscrowTokenFacet` to
`IexecEscrowFacet`.

### Breaking changes

-   Remove native mode contracts: `IexecEscrowNativeFacet`, `IexecEscrowNative`,
    `IexecInterfaceNative`. Only the token mode remains. (#343)
-   Remove the `token` suffix throughout: `IexecEscrowTokenFacet` becomes
    `IexecEscrowFacet`, `IexecEscrowToken` becomes `IexecEscrow`, and
    `IexecInterfaceToken.sol` becomes `IexecInterface.sol`. (#356)
-   Remove `setName` from `Registry` and `RegistryEntry`, together with reverse
    registration support. (#344)
-   Remove Bellecour support. (#341)
-   Merge `IexecPoco1Errors` into `IexecPoco1`. The `IexecPoco1Errors` interface
    and its ABI are gone; `IncompatibleDatasetOrder` is now declared on
    `IexecPoco1`.

### Internal changes

-   Merge `IexecERC20Base` into the escrow facet. (#345)
-   Convert abstract contracts to libs:
    -   `IexecEscrow` => `EscrowLib` and `StakedRlcLib`
    -   `SignatureVerifier` => `CommonLib`,
-   `FacetBase.owner()` now reads the diamond storage through
    `LibDiamond.contractOwner()` instead of an external `IERC5313` call.

### Not shipped by this upgrade

The `Registry` and `RegistryEntry` changes (`setName` removal, migration shim
removal) are **not** delivered here. `AppRegistry`, `DatasetRegistry` and
`WorkerpoolRegistry` are standalone contracts, not diamond facets, and the
`App` / `Dataset` / `Workerpool` masters are fixed at registry construction. A
`diamondCut` cannot reach them.

Those changes take effect only on a fresh chain deployment, or through a
separate registry migration that deploys new registries chained to the current
ones via `initialize(_previous)` and repoints the proxy with `changeRegistries`.
That migration changes the registry addresses and is out of scope for v7.0.0.

## Pre-upgrade checklist

-   [ ] Full test suite passes (`npm test`).
-   [ ] Storage layout check passes (`npm run check-storage-layout`).
-   [ ] Dry run on a fork of Arbitrum Sepolia (`./dry-run.sh`), deployment diff reviewed.
-   [ ] Dry run on a fork of Arbitrum Mainnet, deployment diff reviewed.
-   [ ] Deployer wallet funded with enough ETH on the target network.
-   [ ] `UPGRADE_SCRIPT` in `.github/workflows/upgrade-facets.yml` set to `v7.0.0.ts`.
-   [ ] Facet addresses in `v7.0.0.ts` match `deployments/<network>/` for the target chain.
-   [ ] Re-check which facets actually changed if the branch moved since this report was written.

## Post-upgrade checklist

-   [ ] Facet list refreshed on the block explorer ("Is this a proxy?").
-   [ ] Artifacts PR merged.
-   [ ] SDK and subgraph notified of the breaking changes.

## Deployment logs

### Arbitrum Sepolia

```
Logs here
```

### Arbitrum Mainnet

```
Logs here
```
